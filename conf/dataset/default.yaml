# @package _global_
dataset:
  noisy_data_paths:
    - ./datasets
  input_pre_post_frame: 3    # N: number of pre/post frames
  edge_pre_post_frame: null  # M: edge frames (null = same as N)
  pre_post_omission: 0
  blind_pixel_ratio: 0.005
  blind_pixel_method: replace
  edge_gaussian_size: 9
  edge_gaussian_sigma: 1.0
  edge_source_frame: mean
